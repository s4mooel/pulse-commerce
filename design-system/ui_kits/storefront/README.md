# Storefront — UI Kit

The shopper-facing experience Pulse renders for end consumers. This is what a customer sees, branded as the merchant. We use a generic neutral storefront here as the kit baseline.

## Components
- `Sf.jsx` — header, hero, product grid, PDP, cart drawer
