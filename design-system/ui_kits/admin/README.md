# Admin / Internal Tools — UI Kit

The Pulse-staff-facing console. Tenant management, support, billing oversight. Procedural, neutral. Same shell vocabulary as the merchant dashboard but visually distinct (subtle ink-tinted topbar) so staff always know which surface they're in.

## Components
- `Admin.jsx` — shell + tenants table + support ticket queue
